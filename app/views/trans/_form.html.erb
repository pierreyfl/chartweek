<%= form_for(@tran) do |f| %>
  <% if @tran.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @tran.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stake %><br>
    <%= f.text_field :stake %>
  </div>
  <div class="field">
    <%= f.label :winnings %><br>
    <%= f.text_field :winnings %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>